# Set true = mock mode (no backend needed)
# Set false = connect to real API at NEXT_PUBLIC_API_URL
NEXT_PUBLIC_USE_MOCK=true

NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Auto-login credentials (used when not authenticated — skips login screen)
NEXT_PUBLIC_DEV_EMAIL=admin@netindavoid.dev
NEXT_PUBLIC_DEV_PASSWORD=ChangeMe!2024
